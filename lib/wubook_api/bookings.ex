defmodule WubookAPI.Bookings do
  @moduledoc """
  API Methods for Booking operations
  """
  alias WubookAPI.Token
  # alias WubookAPI.Query

  def push_activation(%Token{token: _token, lcode: _lcode}, _args) do
  end

  def push_url(%Token{token: _token, lcode: _lcode}, _args) do
  end

  def fetch_new_bookings(%Token{token: _token, lcode: _lcode}, _args) do
  end

  def mark_bookings(%Token{token: _token, lcode: _lcode}, _args) do
  end

  def fetch_bookings(%Token{token: _token, lcode: _lcode}, _args) do
  end

  def fetch_bookings_codes(%Token{token: _token, lcode: _lcode}, _args) do
  end

  def fetch_booking(%Token{token: _token, lcode: _lcode}, _args) do
  end

  def get_fount_symbols(%Token{token: _token, lcode: _lcode}, _args) do
  end

  def cancel_reservation(%Token{token: _token, lcode: _lcode}, _args) do
  end

  def new_reservation(%Token{token: _token, lcode: _lcode}, _args) do
  end
end
